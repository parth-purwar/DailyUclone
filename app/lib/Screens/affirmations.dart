import 'package:flutter/material.dart';

class Affirmations extends StatelessWidget {
  const Affirmations({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Affirmations Coming soon!'),
      ),
    );
  }
}
